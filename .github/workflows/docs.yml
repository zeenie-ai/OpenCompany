# Deploy Mintlify docs on push to docs-OpenCompany/.
name: Deploy Docs

on:
  push:
    branches: [main]
    paths:
      - 'docs-OpenCompany/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install -g mintlify
      - working-directory: docs-OpenCompany
        env:
          MINTLIFY_TOKEN: ${{ secrets.MINTLIFY_TOKEN }}
        run: mintlify deploy
