Hover hint on icon buttons and dense controls.

```jsx
<Tooltip label="Override all agent nodes to use the selected model">
  <ActionButton intent="config" iconOnly><Icon name="Repeat" size={14} /></ActionButton>
</Tooltip>
```

- Tooltip copy may be a full sentence (product convention), but keep it one line.
