Loading arc — validating keys, deploying, model calls.

```jsx
<span style={{ display: 'flex', alignItems: 'center', gap: 8 }}>
  <Spinner size={14} color="var(--dracula-cyan)" />
  <span style={{ fontFamily: 'var(--font-mono)', fontSize: 12, color: 'var(--fg-muted)' }}>Deploying...</span>
</span>
```
