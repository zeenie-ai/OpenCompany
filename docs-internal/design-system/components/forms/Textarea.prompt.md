Multi-line field — agent system prompts, skill instructions, JSON payloads.

```jsx
<Textarea placeholder="You are a helpful assistant that..." rows={4} />
<Textarea mono placeholder='{"query": "..."}' rows={3} />
```
