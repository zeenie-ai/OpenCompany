Progress bar — model downloads (Ollama pulls), batch runs.

```jsx
<Progress value={64} label="Pulling llama3.1:8b" color="var(--dracula-cyan)" />
```

- The accent fill carries a soft glow — same "light = activity" rule as node pulses.
